{
    "name": "WhatappProjects",
    "description": "Whatsapp User Bot",
    "repository": "https://github.com/abdullah5151/WhatappProjects",
    "stack": "container",
    "env": {
        "YOURNAME": {
            "description": "İsim (ŞART DEĞİL)",
            "required": true
        }
    },
    "addons": [{
        "plan": "heroku-postgresql"
    }],
    "buildpacks": [{
        "url": "heroku-community/apt"
    }]
}
